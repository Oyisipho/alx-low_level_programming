#include "main.h"
/**
 * memcpy- a function that copy memory
 * @dest:memory storage
 * @src:copied memory
 * @n: number of bytes
 *
 * return: copied memory with n bytes changed
 */

char*_memcpy(char*dest, char*src, unsigned int n)
{
	int r=o;
	int i=n

for (; r<i, r++)
{
dest[r]=src[r];
n--;
}
return (dest);
}
